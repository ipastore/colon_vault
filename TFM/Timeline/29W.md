    global_mean = {
        "mean_ate": float(np.mean(mean_ates)),
        "std_mean_ate": float(np.std(mean_ates)),
        "median_ate": float(np.mean(median_ates)),
        "mean_aoe": float(np.mean(mean_aoes)),
        "std_mean_aoe": float(np.std(mean_aoes)),
        "median_aoe": float(np.mean(median_aoes)),
        "mean_colmap_images": float(np.mean(mean_colmap_images)),
        "mean_colmap_ratio_loc_frames": float(np.mean(mean_colmap_ratio_images)),
        "mean_ratio_loc_frames": float(np.mean(mean_ratio_frames_loc)),
        "mean_matched_poses": float(np.mean(mean_matched_poses)),
        "mean_no_gt_pct": float(np.mean(mean_no_gt_pct)),
        "mean_trans_rpe_1frame": float(np.mean(mean_rpe_mm_1frame)),
        "std_trans_rpe_1frame": float(np.std(mean_rpe_mm_1frame)),
        "mean_rot_rpe_deg_1frame": float(np.mean(mean_rpe_deg_1frame)),
        "std_rot_rpe_deg_1frame": float(np.std(mean_rpe_deg_1frame)),
        "mean_trans_rpe_10frame": float(np.mean(mean_rpe_mm_10frame)),
        "std_trans_rpe_10frame": float(np.std(mean_rpe_mm_10frame)),
        "mean_rot_rpe_deg_10frame": float(np.mean(mean_rpe_deg_10frame)),
        "std_rot_rpe_deg_10frame": float(np.std(mean_rpe_deg_10frame)),
        "mean_trans_rpe_20frame": float(np.mean(mean_rpe_mm_20frame)),
        "std_trans_rpe_20frame": float(np.std(mean_rpe_mm_20frame)),
        "mean_rot_rpe_deg_20frame": float(np.mean(mean_rpe_deg_20frame)),
        "std_rot_rpe_deg_20frame": float(np.std(mean_rpe_deg_20frame)),
        "mean_trans_rpe_40frame": float(np.mean(mean_rpe_mm_40frame)),
        "std_trans_rpe_40frame": float(np.std(mean_rpe_mm_40frame)),
        "mean_rot_rpe_deg_40frame": float(np.mean(mean_rpe_deg_40frame)),
        "std_rot_rpe_deg_40frame": float(np.std(mean_rpe_deg_40frame)),
        "cp_mean_acc": float(np.mean(mean_cp_mean_acc)),
        "std_cp_mean_acc": float(np.std(mean_cp_mean_acc)),
        "cp_median_acc": float(np.mean(mean_cp_median_acc)),
        "cp_mean_completion": float(np.mean(mean_cp_mean_comp)),
        "std_cp_mean_completion": float(np.std(mean_cp_mean_comp)),
        "cp_median_completion": float(np.mean(mean_cp_median_comp)),
        "num_sequences": len(mean_ates),
    }
    
    Sequence
	Distance (mm) 
	Colmap Frames 
	Colmap TFR(%) 
	SLAM TFR (%) 
	Frames align 
	No GT(%) 
	Mean ATE(mm) 
	RPE(mm) (d=40) 
	RPE(deg) (d=40) 
	Mean Acc(mm) 
	Mean Comp(mm)
    
    

Global 
Mean trajectory distance (mm): 70.4685
Mean Colmap loc images: 665
Mean Colmap ratio loc images: 82.05
Mean SLAM ratio loc frames: 103.80
Mean number of matched images: 659
Mean not GT: 21.11
Mean ATE for all sequences: 4.79\pm4.68
Mean RPE (mm) (d=40) for all sequences: 3.05\pm2.41
Mean RPE (deg) (d=40) for all sequences: 2.19\pm0.93
Mean CloudPoint Accuracy for all sequences: 1.59\pm1.13
Mean CloudPoint Completeness for all sequences: 5.56\pm2.97

{


```bash
"mean_ate": float(mean_ate),
"median_ate": float(median_ate),
"p95_ate": float(p95_ate),
"max_ate": float(max_ate),
"coverage": float(total_coverage) * 100.0,# Change 0 to 1 ratio to 0 to 100 percentage
"num_frames": total_frames,
"trans_rpe_1frame": float(mean_trans_rpe_1frame),
"rot_rpe_deg_1frame": float(mean_rot_rpe_deg_1frame),
"trans_rpe_10frame": float(mean_trans_rpe_10frame),
"rot_rpe_deg_10frame": float(mean_rot_rpe_deg_10frame),
"trans_rpe_20frame": float(mean_trans_rpe_20frame),
"rot_rpe_deg_20frame": float(mean_rot_rpe_deg_20frame),
"cp_mean_acc": float(cp_mean_acc) if cp_mean_acc else 0,
"cp_median_acc": float(cp_median_acc) if cp_median_acc else 0,
"cp_mean_completion": float(cp_mean_completion) if cp_mean_completion else 0,
"cp_median_completion": float(cp_median_completion) if cp_median_completion else 0,
"cp_completion_ratio_1mm": float(cp_completion_ratio_1mm) * 100.0 if cp_completion_ratio_1mm else 0, # Change 0 to 1 ratio to 0 to 100 percentage
"cp_completion_unit_1mm": float(cp_completion_unit_1mm) if cp_completion_unit_1mm else 0,
"cp_completion_ratio_2mm": float(cp_completion_ratio_2mm) * 100.0 if cp_completion_ratio_2mm else 0, # Change 0 to 1 ratio to 0 to 100 percentage
"cp_completion_unit_2mm": float(cp_completion_unit_2mm) if cp_completion_unit_2mm else 0,
"cp_completion_ratio_5mm": float(cp_completion_ratio_5mm) * 100.0 if cp_completion_ratio_5mm else 0, # Change 0 to 1 ratio to 0 to 100 percentage
"cp_completion_unit_5mm": float(cp_completion_unit_5mm) if cp_completion_unit_5mm else 0,
}
```


# 24-03-26
Fixing WP-10-12 after merge with main (waiting for github action)
Send new .pt to morlana
Launch tb in ecs in another port

START WRITING

Host ecs
  HostName 155.210.155.85
  User ecs

/Users/ignaciopastorebenaim/Library/CloudStorage/GoogleDrive-ipastore@unizar.es/My Drive/papers