# 22-05-25
EQAx2
RUben TFM
mail

Estoy haciendo los th sweep

vbm0:
1. [x] no_mask; Gauss2
2. [x] camera_mask; Gauss2
3. [x] specular_camera_mask; Gauss2
4. [x] no_mask; SuperpointNet
5. [x] camera_mask; SuperpointNet
6. [x] specular_camera_mask; SuperpointNet
7. [x] no_mask; MagicLeap
8. [x] camera_mask; MagicLeap
9. [x] specular_camera_mask; MagicLeap

DEspues probe specular_camera_mask; Gauss2 con vbm1,3 y 6

AL analizar rapido estos resultados hay dos conclusiones para anotar en las slides de la [[slides_for_4W_meeting]]

Ademas se debería mejorar la aplicación de la mascara especular (bajar el thresh de blancos y bajarle el erosion para hacerla más exacta)

Además debería probar un sweep pero con border margin 6? EStoy probando con 0,1,3,6. Con 0 lo que pasa es que a veces cae muy sobre la máscara. 

La máscara si se agranda con el valid_border_margin. 1 me parece uqe es muy poquito, 3 está bien
Acordarse que esto se debería repetir en el warped pair quizás.

Conclusión: modifiqupe para primero pasar la máscara con la imagen original y luego hacer los crops, quedó mejor. Ahora estoy probando el valid_border_margin: con th 003y vbm3, le pegaba todavía a las esquina de la máscara

vamos con th 005, vbm6

Otra cosa es que para las otras imagenes hay que hacer un crop o utilizar otra camera_mask (tienen otro tamaño)

# 23-09-25
EStoy viendo en Tensorboard que ya estoy exportando la valid_mask con la specular.

IMPORTANTE: 
train-warped_valid_mask no está agarrando la specular mask pero está agrrando la camera_mask. Lo mismo pasa con val-warped_valid_mask

Entonces lo que estoy entrenando no estaría 100% bien

## Qué cambiar: DONE

**En warped_pair enable:**

```python
# Around line 515 in warped_pair section
if self.config['warped_pair']['enable']:
    # ...existing homography setup...
    
    # photometric augmentation from original image
    # warp original image
    warped_img = torch.tensor(img_o, dtype=torch.float32)
    warped_img = self.inv_warp_image(warped_img.squeeze(), inv_homography, mode='bilinear').unsqueeze(0)
    # ...existing photometric processing...
    
    # warped_labels processing...
    # ...existing warped_labels code...
    
    # FIX: Use extra mask for warped_valid_mask instead of just geometric valid mask
    valid_mask = self.compute_valid_mask_with_extra_mask(
        specular_mask, 
        inv_homography=inv_homography,
        erosion_radius=self.config['warped_pair']['valid_border_margin']
    )
    input.update({'warped_valid_mask': valid_mask})
    input.update({'homographies': homography, 'inv_homographies': inv_homography})
```

**En enable_homo_train**

©python
# Around line 447 in homographic augmentation section
if (self.enable_homo_train == True and self.action == 'train') or (self.enable_homo_val and self.action == 'val'):
    # ...existing homography setup...
    
    warped_img = self.inv_warp_image(img_aug.squeeze(), inv_homography, mode='bilinear').unsqueeze(0)
    
    # ...existing warped_labels processing...
    
    # FIX: Use extra mask instead of just geometric valid mask
    valid_mask = self.compute_valid_mask_with_extra_mask(
        specular_mask,
        inv_homography=inv_homography, 
        erosion_radius=self.config['augmentation']['homographic']['valid_border_margin']
    )
    
    input.update({'image': warped_img, 'labels_2D': warped_labels, 'valid_mask': valid_mask})


## Ahora me pongo con la demo

DONE:
agregué step mode
el official superpoint model más las opciones sin implementarlas
keypoint drawing
### Que tengo que hacer para que funcione piola el CODEX DONE

SIEMPRE tiene que hacer una nueva branch para cada cambio. Si hay uncommited changes stash
SIEMPRE antes de empezar tiene que activar py38-sp

No está pudiendo correr la demo:   Tests: ./demo_superpoint.py --config configs/config.yaml --no_display (fails — process killed by macOS seatbelt in this sandbox;
  please rerun locally).

 ./demo_superpoint.py --config configs/config.yaml --no_display (fails: terminated by macOS seatbelt in this sandbox; please re-run locally).

Ya modifiqué el AGENTS.md para que trabaje así, además hay que habilitar el codex con todos los approvals.


# 24-09-25
Cómo empezar hoy?

Revisar el último run de train
Correr otro run solo con la camera_mask sin la specular, mismo lr
Empezar a ver como hago las slides

CAmbios en demo que estarían buenos;
2- Real-time Parameter tuning
8- Avanzar con space y retrasar (quizás cambiar con atras y adelante con las flechas)
6-Agregar SuperpointNet: Leon
6- Agregar SuperpointNet_gauss2: ultimo train 
x- agregar el nombre del frame, la cantidad de keypoints, los th the conf y nn
9- Hacer un dashboard con las advanced metrics

Como arranco: 
export sin specular_mask
joint training sin specular_mask
Averiguar issue de errores en training
Agregar las otras features


[[4W_weekly_meeting]]

ver lo de la matriculacion

Algunas conclusiones para hacer segun la reunion:

- Debería poder ver que tira igual a las labels en el train set (fijarse como mostrar eso)
- Probar con otra secuencia para entrenar
- Al video hacerle el crop para que no tire en los bordes: DONE
- Tengo que poner un condicional para aplicar la máscara especular en training o no
- Desambiguar el vbm de la camera_mask y de la specular_mask
# 25-09-25
hay que tener en cuenta una cuestion: el no mask when training se hace con la train image con su máscara. 

Hay que poner condicionales para mascara en export
Mascara en training solo en train-image
Mascara en training solo en warp-image
Mascara en training en las dos train-image y warp-image

De todas maneras creo que está bien, porque le estas diciendo: Mira acá no hay labels, quizás si le sacás la máscara algun que otra label va a caer ahí y va a distorsionar

para agregar más información hayuna feature request advanced como hacer un histograma de tracks por imagen.

TODO acumulado con orden de prioridad : 

- Poner condicionales de mask en export, training_train_image; training_warp_image
- Desambiguar vbm de la camera_mask y hacer más fina la máscara especular
- Cropear todos los videos para 960 1344
- Exportar: Entrenar con más datos: dejar intacto la 33 para train, sumar otra secuencia de video y hacer un random sampling de otra secuencia para val.
- Hacer un export D con el th muy bajo para hacer el ablation study de la mascara especular
- Probar con AdamW
- Hacer informes de tracking
- Averiguar si el heatmp está normalizado
# 26-09-25

Voy a empezar con la propuesta de TFM
Me puse con el tema de la matriculación 
Y al final pude: 
- Desambiguar las mascaras con erode_specular_mask, erode_camera_mask y valid_border_margin
- Además puse una flag para mandarle la máscara solo a la source image o a la warped image. Si quiero exportar con máscara pero entrenar si n máscara en la source, puedo desactivar la flag en el yaml de train
- Adapté todo para desarrollar en MACOS con mps

# 27-09-25

Empezar con: 
- Poner los nombres de los experimentos y ver las demos, organizar la sheet
- Procesar todos los videos con ffmpeg, podría ver si necesito pasar toso a frames o no. O solo procesar algunos y listo
- Armar el set D, con más imagenes de entrenamiento, un set de validacion a parte y un st de test aparte.
- Con ese D hacer un D con th alto y otro con th normal (podría hacerse otro con th bajo)
- A partir de ahí ver que run de training hago, anotarlo en la sheet
- Cuando eso este andando puedo avergiuar lo del heatmap a ver si está normalizado
- Y ahí ver de agregar un informe de tracking

# 29-09-25
De todos los sweeps
th= 0.75
vbm no cambio casi nada, dejarlo en 3 por default
specular=0
camera_mask=10

la flag tiene que ser de la combinated mask, no solo de la camera
