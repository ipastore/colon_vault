# 01-11-25

Empiezpo debugeando la ultima tool para catchear info.
Debugueado y arreglado. 

Ahora queda hacer el script de endomapper.py

# 02-12-25
Seguir con el endompapper.py
Ya estoy debugueando y cargué todo con data y pred faltaría el ground truth
# 03-12-25

Script para exportar los matches de los keyframes
```python
#!/usr/bin/env python3
import argparse, subprocess, re, sys, shutil
import numpy as np
from pathlib import Path

def q_to_R(qw,qx,qy,qz):
    xx,yy,zz = qx*qx,qy*qy,qz*qz
    xy,xz,yz = qx*qy,qx*qz,qy*qz
    wx,wy,wz = qw*qx,qw*qy,qw*qz
    return np.array([
        [1-2*(yy+zz), 2*(xy-wz),   2*(xz+wy)],
        [2*(xy+wz),   1-2*(xx+zz), 2*(yz-wx)],
        [2*(xz-wy),   2*(yz+wx),   1-2*(xx+yy)]
    ], dtype=np.float64)

def parse_cameras(path):
    cams={}
    with open(path,'r',encoding='utf-8') as f:
        for ln in f:
            if not ln.strip() or ln.startswith('#'): continue
            p=ln.split()
            if len(p)<4: continue
            cam_id=int(p[0]); width=int(p[2]); height=int(p[3])
            cams[cam_id]=(width,height)
    return cams

def parse_points3d(path):
    pts={}
    with open(path,'r',encoding='utf-8') as f:
        for ln in f:
            if not ln.strip() or ln.startswith('#'): continue
            p=ln.split()
            if len(p)<7: continue
            pid=int(p[0]); X=float(p[1]); Y=float(p[2]); Z=float(p[3])
            pts[pid]=(X,Y,Z)
    return pts

def parse_images(path):
    imgs=[]
    with open(path,'r',encoding='utf-8') as f:
        lines=[ln.rstrip('\n') for ln in f if ln.strip() and not ln.startswith('#')]
    i=0
    while i<len(lines):
        p=lines[i].split()
        if len(p)<10: i+=1; continue
        d={
            'image_id':int(p[0]),
            'qw':float(p[1]),'qx':float(p[2]),'qy':float(p[3]),'qz':float(p[4]),
            'tx':float(p[5]),'ty':float(p[6]),'tz':float(p[7]),
            'camera_id':int(p[8]),
            'name':" ".join(p[9:])
        }
        obs=[]
        if i+1<len(lines):
            q=lines[i+1].split()
            for k in range(0,len(q),3):
                try:
                    u=float(q[k]); v=float(q[k+1]); pid=int(float(q[k+2]))
                    obs.append((u,v,pid))
                except: pass
        d['points2d']=obs
        imgs.append(d)
        i+=2
    return imgs

def ffmpeg_extract_scaled_from_video(video_path, frame_index, out_path, W=720, H=540):
    vf = f"select='eq(n\\,{frame_index})',scale={W}:{H}"
    cmd = ["ffmpeg","-hide_banner","-loglevel","error","-y",
           "-i", str(video_path), "-vf", vf, "-vsync","0","-frames:v","1", str(out_path)]
    subprocess.run(cmd, check=True)

def ffmpeg_scale_image(src_path, dst_path, W=720, H=540):
    cmd = ["ffmpeg","-hide_banner","-loglevel","error","-y",
           "-i", str(src_path), "-vf", f"scale={W}:{H}", str(dst_path)]
    subprocess.run(cmd, check=True)

def ffmpeg_preextract_all_frames(video_path, tmp_dir, W=720, H=540):
    tmp_dir.mkdir(parents=True, exist_ok=True)
    cmd = ["ffmpeg","-hide_banner","-loglevel","error","-y",
           "-i", str(video_path),
           "-vf", f"scale={W}:{H}",
           "-vsync","0","-start_number","0",
           str(tmp_dir / "%08d.png")]
    subprocess.run(cmd, check=True)

def draw_overlay(base_path, points_uv, src_size, out_path, radius=2, color=(255,0,0,140)):
    from PIL import Image, ImageDraw
    Wsrc,Hsrc = src_size
    Wdst,Hdst = 720,540
    sx = Wdst/float(Wsrc); sy = Hdst/float(Hsrc)
    base = Image.open(base_path).convert("RGBA")
    overlay = Image.new("RGBA", (Wdst,Hdst), (0,0,0,0))
    draw = ImageDraw.Draw(overlay)
    for (u,v) in points_uv:
        x = int(round(u*sx)); y = int(round(v*sy))
        if 0 <= x < Wdst and 0 <= y < Hdst:
            draw.ellipse((x-radius, y-radius, x+radius, y+radius), fill=color)
    Image.alpha_composite(base, overlay).convert("RGB").save(out_path)

def process_one_map(map_id:int, map_dir:Path, out_root:Path, args, pre_frames_dir:Path=None):
    cameras_txt = map_dir / "cameras.txt"
    images_txt  = map_dir / "images.txt"
    points3D_txt= map_dir / "points3D.txt"
    if not (cameras_txt.exists() and images_txt.exists() and points3D_txt.exists()):
        print(f"[{map_id}] Saltado: faltan ficheros en {map_dir}")
        return

    cams = parse_cameras(cameras_txt)
    imgs = parse_images(images_txt)
    pts3d= parse_points3d(points3D_txt)

    outdir = out_root / f"{map_id}"
    outdir.mkdir(parents=True, exist_ok=True)

    numeric_name = re.compile(r"^[0-9]+$")

    for img in imgs:
        img_id=img['image_id']; cam_id=img['camera_id']; name=img['name']
        if cam_id not in cams:
            print(f"[{map_id}] WARN: camera_id {cam_id} no está en cameras.txt; salto {img_id}")
            continue
        Wsrc,Hsrc=cams[cam_id]

        # número <n>
        if args.number_from == "image_id":
            n = img_id; src_is_index = bool(numeric_name.match(name))
        elif args.number_from == "name":
            if numeric_name.match(name): n, src_is_index = int(name), True
            else: n, src_is_index = img_id, False
        else:
            if numeric_name.match(name): n, src_is_index = int(name), True
            else: n, src_is_index = img_id, False

        # --- Depth sparse ---
        R = q_to_R(img['qw'],img['qx'],img['qy'],img['qz'])  # world->cam
        t = np.array([img['tx'],img['ty'],img['tz']], dtype=np.float64)
        depth = np.zeros((Hsrc,Wsrc), dtype=np.float32)
        pts_uv = []
        for (u,v,pid) in img['points2d']:
            if pid == -1 or pid not in pts3d: continue
            Xw,Yw,Zw = pts3d[pid]
            Xc,Yc,Zc = R @ np.array([Xw,Yw,Zw]) + t
            if Zc <= 0: continue
            ui,vi = int(round(u)), int(round(v))
            if 0 <= ui < Wsrc and 0 <= vi < Hsrc:
                depth[vi,ui] = Zc
                pts_uv.append((u,v))
        np.save(outdir / f"depth_{n}.npy", depth)

        # --- Keyframe base 720x540 (sin puntos) ---
        base_dst = outdir / f"Keyframe_{n}.png"
        if src_is_index:
            if args.video:
                if args.video_strategy == "batch" and pre_frames_dir is not None:
                    src = pre_frames_dir / f"{int(name):08d}.png"
                    if src.exists():
                        shutil.copy2(src, base_dst)
                    else:
                        print(f"[{map_id}] WARN faltó preframe {src.name}; caigo a perframe")
                        ffmpeg_extract_scaled_from_video(args.video, int(name)+int(args.frame_offset), base_dst, 720, 540)
                else:
                    ffmpeg_extract_scaled_from_video(args.video, int(name)+int(args.frame_offset), base_dst, 720, 540)
            else:
                print(f"[{map_id}] WARN NAME numérico y sin --video; no extraigo Keyframe.")
        else:
            if args.frames_dir:
                src = Path(args.frames_dir) / name
                if src.exists():
                    ffmpeg_scale_image(src, base_dst, 720, 540)
                else:
                    print(f"[{map_id}] WARN no existe {src}; no copio Keyframe.")
            else:
                print(f"[{map_id}] WARN NAME no numérico y sin --frames_dir; no copio Keyframe.")

        # --- Overlay opcional ---
        if args.overlay and base_dst.exists() and pts_uv:
            color = tuple(map(int, args.overlay_color.split(",")))
            draw_overlay(base_dst, pts_uv, (Wsrc,Hsrc), outdir / f"KeyframeOverlay_{n}.png",
                         radius=args.overlay_radius, color=color)

        print(f"[{map_id}] n={n} depth=OK img={'OK' if base_dst.exists() else '—'} overlay={'ON' if args.overlay else 'OFF'}")

def main():
    ap=argparse.ArgumentParser(description="Todos los mapas: depth_<n>.npy + Keyframe_<n>.png (720x540); overlay opcional; extracción batch.")
    ap.add_argument("--maps_root", required=True)
    ap.add_argument("--out_root",  required=True)
    ap.add_argument("--video",     help="Vídeo original (para NAME numérico).")
    ap.add_argument("--frames_dir")
    ap.add_argument("--frame_offset", type=int, default=0)
    ap.add_argument("--number_from", choices=["auto","name","image_id"], default="auto")

    # Overlay opcional
    ap.add_argument("--overlay", action="store_true")
    ap.add_argument("--overlay_radius", type=int, default=2)
    ap.add_argument("--overlay_color", default="255,0,0,140")

    # NUEVO: estrategia de extracción
    ap.add_argument("--video_strategy", choices=["batch","perframe"], default="batch",
                    help="batch=decodifica una vez todo el vídeo; perframe=una llamada ffmpeg por frame.")
    ap.add_argument("--preextract_dir", help="Dónde guardar frames 720x540 si usas batch (por defecto: <out_root>/_preframes)")

    args=ap.parse_args()

    maps_root = Path(args.maps_root)
    out_root  = Path(args.out_root)
    out_root.mkdir(parents=True, exist_ok=True)

    # Detectar mapas
    map_dirs = [(int(d.name), d) for d in sorted(maps_root.iterdir(), key=lambda p: p.name)
                if d.is_dir() and d.name.isdigit()]
    if not map_dirs:
        print("No se encontraron mapas en", maps_root)
        sys.exit(1)

    # Pre-extracción si procede
    pre_frames_dir = None
    if args.video and args.video_strategy == "batch":
        pre_frames_dir = Path(args.preextract_dir) if args.preextract_dir else (out_root / "_preframes")
        if not pre_frames_dir.exists() or not any(pre_frames_dir.iterdir()):
            print("[preextract] Decodificando el vídeo una sola vez a 720x540…")
            ffmpeg_preextract_all_frames(args.video, pre_frames_dir, 720, 540)
        else:
            print("[preextract] Reutilizando frames ya preextraídos:", pre_frames_dir)

    for mid, mdir in map_dirs:
        process_one_map(mid, mdir, out_root, args, pre_frames_dir)

    print("\nDONE.")

if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        sys.exit(130)
```

# 04-12-25

De la meeting de ayer: 

- Implementar la gt
- Ver como puedo incorporar visualizaciones. Idealmente deberían ir en el Tensorboard
- hacer el push de mi branch


Added reporting of depths per KF
Added 3D-2D observations in points3D.txt
Added reporting of Cudasift features (kpid, x, y, scale, ori, score, descriptor)

# 05-12-25
Arreglar image_size
armar la segunda fucion con use_sparse_depth: false

• Here’s a compact recap to start fresh on Task 2 (sparse depth ground truth):

  - We worked on Endomapper/LightGlue alignment:
      - Stored per-image sizes in Endomapper NPZ and passed image_size to views.
      - Converted SIFT orientations to radians in endomapper.py.
      - Prefix handling for checkpoints: matcher.* and matcher config set to input_dim=128, add_scale_ori=True to match official LightGlue weights (SIFT).
      - Conversion script now verifies loading with a minimal pipeline and matcher config matching the weights.
      - Documentation added: docs/theory/lightglue_checkpoint_mismatch.md explaining prefix/input_dim/add_scale_ori mismatches.
      - For training config sift+lightglue_endomapper_homography.yaml, set matcher.add_scale_ori: true, input_dim: 128.
  - Matching behavior:
      - Reordering keypoints via top-k is fine; LightGlue is permutation-equivariant (order doesn’t matter if attributes stay aligned).
      - LightGlue matches any keypoints; match counts can exceed count of 3D-labeled points; valid_3D_mask and depth masks differ because 3D IDs and depth come from different sources.
      - Orientations should be radians for LightGlue positional encoding.
  - Current branch: feature/sparse-depth-matcher.
  - Pending tracked edits (besides docs/endmmapper): gluefactory/configs/sift+lightglue_megadepth.yaml, gluefactory/settings.py, gluefactory/train.py, gluefactory/utils/experiments.py are dirty from
    earlier user changes (not mine).
  - Conversion script path: tools/convert_pth_to_tar.py.
  - Docs added: docs/theory/lightglue_checkpoint_mismatch.md.

  Ready to proceed with Task 2: implement sparse depth ground truth/matcher.

# 06-12-25
EStuve arreglando mucos problemas con add_scale_oris, si los extractores usan degree o rad + que tipo de scale usan. Ya está todo en línea. 

Ahora estuve debugeuando las funciones de sparse depth
-1 Cuando se usa el project, se encuentran pocos matches. Los Gt deberían ser 130 y encuentra 8, para sacar el 25% de los kp se necesita un thresh de 15px o algo así (hay una parte de debugeo para prinetarl)
-2 La parte de positive funciona bien sin la sparse_depth, pero encuentra pocos negatives, por que? 

Quedan dos cosas: 
- 1 hacer la funcion de project con kannala si se quiere usar postive_depth para positive
- Hacer la funcion de epipolar symetric pero con kannala
- Aumentar el th: para esto podría ver la distribución de de loas epi_dist. Puedo hacer una funcion que me saque los cuantiles de los torch alojados en cuda para printear que threshold debería ser. OK 
- Visualizar. TODO

# 07-12-25

## Endomapper
### Witho project (sparse_depth) without kannala
```txt
[reproj] batch 0 overlap_0to1=0.5039
[reproj] batch 0 kp0->1 total=360
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [4, 8, 21, 32, 41]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [356, 352, 339, 328, 319]
[reproj] batch 0 kp1->0 total=196
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [3, 6, 17, 24, 31]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [193, 190, 179, 172, 165]
[reproj] batch 1 overlap_0to1=0.1141
[reproj] batch 1 kp0->1 total=209
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [0, 0, 0, 0, 0]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [209, 209, 209, 209, 209]
[reproj] batch 1 kp1->0 total=323
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [0, 0, 0, 0, 0]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [323, 323, 323, 323, 323]
[epi] batch 0 overlap_0to1=0.5039
[epi] batch 0 kp0->1 total=732
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [705, 715, 716, 722, 722]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [27, 17, 16, 10, 10]
[epi] batch 0 kp1->0 total=832
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [782, 806, 813, 820, 822]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [50, 26, 19, 12, 10]
[epi] batch 1 overlap_0to1=0.1141
[epi] batch 1 kp0->1 total=687
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [662, 676, 687, 687, 687]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [25, 11, 0, 0, 0]
[epi] batch 1 kp1->0 total=651
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [593, 603, 603, 603, 603]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [58, 48, 48, 48, 48]
```

### sparse_map (witout_depth)
```bash
[12/07/2025 15:48:34 gluefactory INFO] Starting epoch 0
[epi] batch 0 overlap_0to1=0.5039
[epi] batch 0 kp0->1 total=905
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [872, 886, 887, 894, 895]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [33, 19, 18, 11, 10]
[epi] batch 0 kp1->0 total=905
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [876, 890, 891, 897, 899]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [29, 15, 14, 8, 6]
[epi] batch 1 overlap_0to1=0.1141
[epi] batch 1 kp0->1 total=982
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [956, 975, 982, 982, 982]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [26, 7, 0, 0, 0]
[epi] batch 1 kp1->0 total=982
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [896, 905, 905, 906, 906]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [86, 77, 77, 76, 76]
```

## Megadepth

### max=1024
```text
[12/07/2025 15:57:33 gluefactory INFO] Starting epoch 0
[reproj] batch 0 overlap_0to1=0.3894
[reproj] batch 0 kp0->1 total=287
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [41, 62, 71, 78, 85]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [246, 225, 216, 209, 202]
[reproj] batch 0 kp1->0 total=426
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [41, 62, 71, 79, 85]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [385, 364, 355, 347, 341]
[reproj] batch 1 overlap_0to1=0.5506
[reproj] batch 1 kp0->1 total=427
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [249, 269, 274, 286, 291]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [178, 158, 153, 141, 136]
[reproj] batch 1 kp1->0 total=465
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [249, 268, 273, 286, 292]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [216, 197, 192, 179, 173]
[epi] batch 0 overlap_0to1=0.3894
[epi] batch 0 kp0->1 total=628
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [313, 353, 383, 391, 400]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [315, 275, 245, 237, 228]
[epi] batch 0 kp1->0 total=647
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [596, 623, 638, 639, 640]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [51, 24, 9, 8, 7]
[epi] batch 1 overlap_0to1=0.5506
[epi] batch 1 kp0->1 total=434
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [264, 294, 307, 316, 321]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [170, 140, 127, 118, 113]
[epi] batch 1 kp1->0 total=587
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [203, 211, 216, 217, 217]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [384, 376, 371, 370, 370]
```

### max=2048
```text

[12/07/2025 16:02:13 gluefactory INFO] Starting epoch 0
[reproj] batch 0 overlap_0to1=0.3894
[reproj] batch 0 kp0->1 total=478
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [87, 128, 150, 170, 183]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [391, 350, 328, 308, 295]
[reproj] batch 0 kp1->0 total=951
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [87, 128, 150, 173, 189]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [864, 823, 801, 778, 762]
[reproj] batch 1 overlap_0to1=0.5506
[reproj] batch 1 kp0->1 total=919
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [587, 626, 654, 679, 708]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [332, 293, 265, 240, 211]
[reproj] batch 1 kp1->0 total=1376
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [587, 627, 666, 742, 855]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [789, 749, 710, 634, 521]
[epi] batch 0 overlap_0to1=0.3894
[epi] batch 0 kp0->1 total=1415
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [668, 708, 736, 742, 745]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [747, 707, 679, 673, 670]
[epi] batch 0 kp1->0 total=1238
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [1199, 1219, 1225, 1228, 1228]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [39, 19, 13, 10, 10]
[epi] batch 1 overlap_0to1=0.5506
[epi] batch 1 kp0->1 total=822
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [659, 670, 674, 677, 677]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [163, 152, 148, 145, 145]
[epi] batch 1 kp1->0 total=964
counts_lt_thresholds(px) (2, 3, 4, 5, 6): [892, 927, 944, 954, 955]
counts_gt_thresholds(px) (2, 3, 4, 5, 6): [72, 37, 20, 10, 9]
```

