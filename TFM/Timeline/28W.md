# 17-03-26

TODAY: 
- Run finetuning in Endo_3d (ecs-stef). Ran superpoint. Transfering the dataset for ENDO_3D
- Install Roma by source in the docker image
- add all valid pairs
- Run all  megadepth + endomapper
- Add gt matches of the sparse map with viz

Setting up docker image with safe dependencies

**LG**
requires-python = ">=3.6" OK
torch>=1.9.1 OK 
torchvision>=0.3
numpy
opencv-python
matplotlib
kornia>=0.6.11

**ROMA**
requires-python = ">=3.9"
dependencies = [
    "albumentations",
    "einops",
    "h5py",
    "kornia",
    "loguru",
    "matplotlib",
    "opencv-python",
    "poselib>=2.0.4",
    "timm",
    "torch>=2.5.1",
    "torchvision",
    "tqdm",
    "wandb",


[project.optional-dependencies]
fused-local-corr = [
    "fused-local-corr>=0.2.2 ; sys_platform == 'linux'",
]

**Gluefactory**
requires-python = ">=3.6"
dependencies = [
    "torch>=1.7", (LG torch 1.9.1); **(ROMA 2.5.1 )**
    "torchvision>=0.8",
    "numpy",
    "opencv-python",
    "tqdm",
    "matplotlib",
    "scipy",
    "h5py",
    "omegaconf",
    "tensorboard",
    "albumentations",
    "kornia",  (LG>0.6.1)
    "seaborn",
    "joblib",
    "scikit-learn~=1.3.0",
    "lightglue @ git+https://github.com/cvg/LightGlue.git",
]

# 18-03-26
Reunión:

- Hacer mapas nuevos de seqs cortas con COLMAP 4.0. Primero sparse y luego ver si se puede densificar. Para esto debería hacer una imagen de docker para instalar colmap con cuda suport eligiendo el compute power.
- Correr los nuevos pesos en el SLAM: 
	- 03-sift_cudasift+lg_ENDO_HOMO
	- 04-sift_cudasift_lg_MD_3D
	- 05-sift_cudasift_ENDO
	Para esto tengo que convertir los pesos y pasarselo a Morlana.
- Pensar bien por qué en val no tendría influencia el val split.
- Adaptar Dockerfile para que use ci.sh con el experiments.sh para correr 5 veces en vez del unique run


```bash
# 1. Create local branch with new name, tracking the remote
git checkout -b WP-12-10-show-results-wb origin/include_5_runs_Dockerimage

# 2. Push to remote with new name
git push origin -u WP-12-10-show-results-wb

# 3. Delete old remote branch
git push origin --delete include_5_runs_Dockerimage

```
