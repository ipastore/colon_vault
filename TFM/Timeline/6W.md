# 06-10-25

Estoy analizando 15_F con diferentes checkpoints. No veo tanta diferencia en el report de los 75, 240 y 395k.

TODO del día: 
Entender precision, recall
Entender el train_heatmap con el debugging en macos
Leer el repo de Leon
Dejar de lado lo del docker por ahora, encararlo la semana que viene.

EStuve estudiando como son las losses y el export. Me quedé con que hubo un problema con el roi_pool para mps y cpu que estaría solucionado. Ahora hay que entender por qué esos patches dan 0. 

Además puse un breakpoint en train:model_heatmap que es donde llama a get_residual_loss: a partir de ahí hay varios llamados de funciones para entender que se hace

# 07-10-25

metadata que me compartió morlana:
https://docs.google.com/spreadsheets/d/1oaExjN1rlpy9pzuDAjRSyHGfNtlmebP1EjT47S4IHdQ/edit?gid=0#gid=0

**Leon repo**:
- subió todo de un saque el código, dificil ver la cronología.
- main commit contribution in 2024 update where he updated the supercolmap, mainly with sp-lg.
- Hay datasets de Endomapper y de UZ, no se diferencian bien
- Hay un modelo que dice SuperpointNet_notrack. Entonces SUperpointNet_notrack está basado en el SuperpointNet con Batch_normalization. La única diferencia es que no tiene unmétodo que se llama forward_features (no entendería por qué).
- En realidad parece que el modelo SuperpointNet_notracck no lo usa. 
- Porque superpoint_emcolmcap_train_heatmap_xxxx

		xxxx=tgh; tracking_gorup; tracking
		
		xxxx= spec; 
- Hay un .py copado video_tracking que hace un video uno al lado del otro con imágenes. 
- Train_model_frontendes 

train_model_heatmap_spec is for specularities
train_model_heatmap_spec is for specularities + dilation with gaussian_blur

- Lo otra diferencia está en el dataset que en el get_item (creo), carga desde la construcción de COLMAP
en train_model_heatmap_tgh: linea 122: batch_descriptor_loss_tracking

linea 232 is img_list instead of img
linea 290: no se por qué hace un for de img_list dentro del train_model_heatmap. No debería hacerlo en el get_item? o algo parecido?
anoto por ahora que train:model_hetamp_ tgh es como el spec_ga pero sin aplicar las spec, sin el blur pero apicando los loops dentro del script para calcular la loss: for i in range (image_list)
Parece que la tgh es más vieja que el tracking_group porque el tracking_group tiene toda la parte de warping y specularities comentada.

- En sparse_loss.py hay difrencias: 
  linea 420 def batch_descriptor_loss_tracking(descriptors1, descriptors2, correspondences, **options):
- Diferencia importante entre descriptor_loss_sparse y descriptor_loss_tracking en sparse_loss.py: hay un argumento de más que es corresponences. Estos deben ser los matches precomputados del SfM o puede ser otra cosa, OJO. Efectivamente correspondences es pasado como matches_list
- batch_descrptor_loss_tracking es la función llamda por los 3 variaciones de train_model_heatmap_tgh_tracking_tracking_group
- tgh tiene todavía homografía pero con tracking, parece un híbrido. Quizás tgh es Tracking Group Homography. Además utiliza la descriptor_loss_original
- tarcing_group vs tracking: 
	- linea 123 de tracking_group: 
		self.batch = self.config\["data"]\["batch"]
	- linea 247 tracking: tiene descomentado la img2
	- La ultima impresion me dio que el tracking_group es el último porque en el dataest tiene partes comentadas del tracking. Aunque esto me parecezca una forma incorrecta. El get_ittem debería hacer el trabajo.
- En todos los config tiene lr = 0.00001 (un cero más)
- En el dataset de Endomapper_tracking_group linea 571 devuelve un input.update('name': names_list). En el tracking devuelve image1 e image 2 (name 1 y name 2)
- En export fusion hay una diferencia muy importante en line 363:
  export_detector_trackingAdapt_gpu(), esta función carga las labels desde las reconstrucciones de colmap
# 08-10-25

Pensé que entendía el descriptor_loss_ tracking pero creo que no.

La diferencia en la loss de descripcion es que:
```python
uv_a = (correspondences[:, :2]/cell_size).floor_()

uv_b_matches = (correspondences[:, 2:]/cell_size).round_()
```

explain the export_fusion
Also, how is batch_size interacting with this? in the grouping?
explain the supercolmap
How is colmap being supercharged?

Hay dos datasets diferentes importantes. Uno para export otro para training
- Endomapper_COLMAP.py (export)
- Endomapper_tgh.py (training)

Hay una diferencia importante entre el tracking_group y los demas: 
```yaml

data:
# name: 'uz'
dataset: 'EndoMapper_tracking_group' # 'coco'
batch: 4
```

Utiliza el data.batch para iterar sobre las reconstrucciones.

[[6W_weekly_meeting]]
