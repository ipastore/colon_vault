# 08-12-25
Ya tengo los keyframe_id.png extraidos con: 
```bash
python3 tools/extract_frames_depths_matches_endomapper_seq.py --maps_root data/Endomapper_CUDASIFT_DIC25/Seq_001_b/output/3D_maps/  --keyframes --video ~/all_sequences/Seq_001_b.mp4 --number_from name
```

OR

Forr all the seqs
```bash
for seq in data/Endomapper_CUDASIFT_DIC25/Seq_*/; do
seq_name=$(basename "$seq");
python3 tools/extract_frames_depths_matches_endomapper_seq.py \
--maps_root "${seq}output/3D_maps" \
--keyframes \
--video "$HOME/all_sequences/${seq_name}.mp4" \
--number_from name ;
done
```

OR  for specific seqs

```bash
SEQS=("Seq_027_g" "Seq_024_c" "Seq_024_b" "Seq_016_c" "Seq_016_a" "Seq_014_a")
for seq in "${SEQS[@]}"; do
echo "Processing $seq..."
python3 tools/extract_frames_depths_matches_endomapper_seq.py \
--maps_root "data/Endomapper_CUDASIFT_DIC25/${seq}/output/3D_maps" \
--keyframes \
--video "$HOME/all_sequences/${seq}.mp4" \
--number_from name \
--out_root "data/Endomapper_CUDASIFT_DIC25"
done
```
OR with new integrated tool
```bash
python3 tools/preprocess_endomapper.py \
--root data/Endomapper_test \
--video-root ~/all_sequences
```
# 09-12-25
Ayer terminé parte importante de la demo de la gt para entrenar endomapper. 

1- Los KP en realidad son map points, podría informar los keypoints que no fueron padeados
2- Se puede plotear solamente la pred y solamente la ground truth
3- Además se puede plotear el matcheo con la herramienta extract_frame_depths_matches_endomapper.
4- Naming de los archivos.

Por ultimo correr los runs para ver los diferentes ths.

se toma max_num_features 1024 porque la mayoría anda por debajo de eso, solo algunos por arriba.

working

# 10-12-25
1. Entrenar solamente con true positives. (comentando hard negatives the la loss) o ignorarlos todos.
2. Depth escalada (exportar desde EndoCartoscopeSlam multiplicando por la scala.)
3. Kannala Brandt (lightdepth)
4. Para linea epipolar con Kannala: se podrían distorsionar los puntos  o se podría distorsionar la linea (una de dos)
5. Homografía no tiene sentido por no tener implementacion FIABLE de python de CUDASIFT.
6. Preguntar a Morlana qué videos de la short seq usar para training
7. Armar algo medianamente consistente para endomapper de do_evaluation
8. Queda armar una tercer variante de la sparse_depth (o más): Primero cargar los matches ground_truth y encontrar negativos ocn la reproyeccion y/o con la epi_dist. En realidad si luego de implementar kannala, deberian ir bien de cargar los positivos con reproyección.
# 11-12-25

1. [x] Investigar como se incorpora depth correctamente a CUDASIFT.
2. [x] Compilar con release y correr mitad de short_seqs
3. [x] Preprocesar todos los mapas
4. [x] Correr el resto de los mapas de seq para test de SLAM
5. [x] Armar nuevos txt de train seq
6. [x] Armar para que el txt lea la seq en vez de la seq_map
7. [x] Integrar funcion de sparse depth con 4 flags: gt, pos, neg y epi
8. [x] Probar a ver si ahora reproyecto bien con sparse_depth:
		 Da mejor, se puede jugar con los th. Voy a seguir para no perder tanto tiempo
9. [x] Debugear a ver si puedo correr el entrenamiento con todos los mapas
10. [x] pushear
11. [x] builder docker
12. [x] enroot
13. [x] SLRUM
        
        

BUG: l758. and l711. string strNameKF = to_string(pKFi->mnFrameId);



CORRECT: string strNameKF = pKFi->mImageName;

pero porque funcionó en seq_035_b?????!!!! 
Porque Richard lo cambió para sus funciones nada más


Probar idferentes sizes para ver como mandamos con el train y el val a la dgx
Hacer un codex resume para terminar lo del logging

Una vez completados los pasos hata 13 me pongo con un proper do_evaluation

# 12-12-25

para correr con distribuido poner --distributed 

```bash
python -m gluefactory.train gluestick_MD --conf gluefactory/configs/superpoint+lsd+gluestick-megadepth.yaml --distributed
```

El batch_size default de megadepth is 32

## bs=2, null, null
```bash
[12/12/2025 11:11:52 gluefactory INFO] Train epoch stats: size=33492, batch_size=2, drop_last=True, loader_batches=16746, iter_per_epoch=16746.00
[12/12/2025 11:11:52 gluefactory INFO] Val epoch stats: size=5368, batch_size=2, drop_last=False, loader_batches=2684, iter_per_epoch=2684.00

16% OF train/val
```


Con train_num_per_scene > 10 hay algunos y sin filtrar mapas se rompe
Si uso un filtro de por lo menos 10 imágenes llego a 100 pero pierdo muchos samples
DECISION, voy con null null

## Cosas a mejorar 
- en vez de log_every_iter = 1000 lo saco y hago cada epoch y listo. Porque si la epoch masomenos tiene 1000 iters, hago dos veces la val al pedo.

- Puedo guardar las imagenes en disco en vez de hacer el tensorboard. Hay que organizar en carpetas por disco y nombre con epoch_X.png. Solamente imprimir la GT_pos cuando se hace la primera epoch.

- Hay que mejorar las imagenes de val, hacerlas que sea vean mejor y ponerle las métricas. 
- Solo hacer read_image si es distinot de train split

- [ ] do_evaluation: mejorar
- [ ] Implementar Kannala
      

# 13-12-25
1. Fijarse si en la gt_generation estoy pidiendo que tenga 3D, probablemente con valid dentro de project. DEbería ser con padded mask para pedirle solamente que no sea un kp padded, ya que quiero encontrar nueva gt_pos pero que no tenga 3D, ya que el SLAM no lo encontró como un match. Si repito el patrón de Megadepth, solamente me voy a quedar con los matches que tienen valid_depth. 
2. Como consecuencia, enel plot de la GT creo que solamente estoy viendo los TP y FP (verde y rojo) de aquellas predicciones en los puntos que tuve 3D (aunque algunos de ellos terminen siendo ignorados). Por ende, para mejorar el plot: 
		
	**Líneas: SUMO VIOLETA**
	- Verde: TP
	- Rojo: FP (que cae por lo menos en un punto verde). De esta manera se que su match verde no es el mismo que el que indica la pred.
	- Violeta: Son FP que caen los does en puntos ignorados. De esta manera estoy diciendo que son posibles matches que quizás no estoy viendo
	**Puntos: SUMO CYAN**  
	- Verde: GT_pos SLAM
	- Cyan: GT_pos reproj
	- Azul: puntos FN
	- Gris: Ignore
	**Contorno punto: QUEDA IGUAL**
	- Rojo: FP
	- Verde: TP
	- Negro: FP
	- Azul: TN
	
1. En línea con lo anterior, el plot de la GT_pos debería tener matches encontrados con el mapa en VERDE y en CYAN matches encontrados con la reproj. Las puedo poner con líneas de diferente grosor para que si encuentran los mismo, se solapen. 
2. 3era implementación de plot: para do_evaluation(): en vez de a tener dos imágenes arriba y abajo voy a tener arrib a LG_epochX, abajo LG_OOB. 
	**Líneas**:
	Verde: pred == label -> TP
	Violeta: en label pero no en pred -> FN
	 rojo: pred pero no label -> FP
En este gráfico solamente de líneas
prec = TP /(TP+FP) = verde / (verde+rojo)
rec = TP / (TP+FN) = verde / (verde + azul)

En este caso la GT_pos sería TP+FP (verde + azul.

Estoy revisando y creo que hay un BUG. me confundo valid_3D con valid_depth
Entonces 


# 14-12-25
EStoy modificando el grafico de la GT y hay algunos puntos verdes sin el borde de FN y no matcheados.
Creo que los second keypoints traen ruido porque pisan encima el color de los kps

