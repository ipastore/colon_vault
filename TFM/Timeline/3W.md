# 15-09-25
Volvi de mallorca, tengo que hacer lo de [[2W_weekly_meeting]], en resumen. Voy a hacer un paso a paso.

Clonar superpoint original repo
REvisar el dataset que me mando morlana
Correr la demo de superglue con el dataset de morlana
Lanzar un vanilla training con un pequeño dataset de morlana: 
	Exportar HA
		Entrenar jointly detector+descriptor
Volver a correr la demo con el modelo entrenado

Bonus: 
Adaptar codex

Que hice hasta las 12?
EStoy commiteando todos los cambios que me habían quedado

- [x]  Run and modify demo_superglue
- [ ] Check superpoinTorch for Colon dataset
	- [x] Revert changes in utils/utils.py (normalizacion and the dot product of the descriptro)
	- [x] See how could I add optionally the mask
	- [ ] VIsualizing wraped pairs with masks
		- [ ] For these I am making a new yaml file for colon

- [ ] Since, I am delaying I could run Vanilla training without masks using the Coco dataset
- [ ] Run the model with the new weights
- [ ] And then run with the masks...


240 heihgt x320 wide

# 16-09-25
Siguiendo el plan de ayer lo que debería hacer: 

1. Con toy_33: Con el mismo yaml de colon, export sin mask
2. Con toy_33: Con el mismo yaml de colon, joint training
3.  Ver si funciona, exportar .pth
4. Correr demo superglue con nuevo .pth
5. Con toda la secuenia 33: export + training
6. Volver a correr demo superglue con nuevo .pth
7. COn toda la secuencia y las mascaras: para esto falta filtrar antes de exportar y de ser posible realizar la visualizacion para ver que las labels tienen sentido
8. Una vez que tenga eso, tendré un nuevo weight y correre unanueva demo
- [x] Bajar superpoint original y correr demo con video
- [x] correr demo con el .pth toy
- [ ] exportar train, val y entrenar para todas las imagenes de 33
- [ ] volver a correr demo de sp
- [ ] Volver a exportar pero con th 0015
- [ ] Volver a exportar train y val (quizas solo seleccionar 1?)
- [ ] correr demo sp

ME EQUIVOQUE y la demo era de SUPERPOINT no de SUPERGLUE


**Aprendizajes**
	- Como modelo tengo que usar SuperPoint_Net_Pretained para matchear el repo original. Para esto tuve que hardcodear una solucion en model_wrap.py line 344 (se podría mejorar)
	- Debería usar 2 yamls separados, lo que importa es matchear el tamaño de la imagen (con downsize) y los paths. AL final, no para no complicarlo. La conclusion finale s algo así:
		- If exporting:
			- set labels to null
			- set homography_adaptation to true
			- set warped_pair to false
			- set photometric augmentation to false
		- If training:
		- set labels to the path of exported predictions
		- set homography_adaptation to false
		- set warped_pair to true
		- set photometric augmentation to true 
	- Para export me quedo con nms:4, threshold: 0.000075
	- Parece correr bien con y sin la máscara. Da algunos keypoints diferentes pero porque es un proceso estocástico
	- There is no leraning rate warmup
	- 

## Commands

### Exporting
#### General
```bash
python export.py <export task>  <config file>  <export folder> [--outputImg | output images for visualization (space inefficient)]
```
#### Ejemplo Coco
```bash
python export.py export_detector_homoAdapt configs/magicpoint_coco_export.yaml magicpoint_synth_homoAdapt_coco
```

#### Export Utilizado Colon

```bash
python export.py export_detector_homoAdapt configs/superpoint_colon_export.yaml superpoint_colon_export_ds4_non_mask_000075_nms_4 --outputImg
```

### Training
#### General
```bash
python train4.py <train task> <config file> <export folder> --eval
```
#### Ejemplo COCO
```bash
python train4.py train_joint configs/superpoint_coco_train_heatmap.yaml superpoint_coco --eval --debug
```
#### Utilizado Colon

```bash
python train4.py train_joint configs/superpoint_colon_train_heatmap.yaml superpoint_colon_train_ds4_non_mask_000075_nms_4 --eval --debug
```

## Debugging model loader
```bash
# Run without uncommenting the alias first to see the error
python ./test/debug_model_loading.py --model_name SuperPointNet_pretrained

# Then uncomment the alias and run again to see it work
python ./test/debug_model_loading.py --model_name SuperPointNet_pretrained

# Compare with other model variants
python ./test/debug_model_loading.py --model_name SuperPointNet

python ./test/debug_model_loading.py --model_name SuperPointNet_gauss2

# Or test with your config file
python ./test/debug_model_loading.py --config configs/superpoint_colon_train_heatmap.yaml
```

## Rnning superpoint demo
```bash
./demo_superpoint.py assets/Seq_035_b.mp4 --H=480 --W=672 --cuda --conf_thresh=0.000075
```

## Cambios que se podrían probar

**Export**
- N de homografías: como afecta esto
- Se podría subir el thresh
Train
- Probar con los otros dos modelos
Demo sp
- poner Esuperpoint
- poner cualquier otro modelo ya fine-tuneado con sp-pytorch para ver si son compatibles 

### Leon: SuperpointEndoscopy:
#### Export yaml
```yaml
100 homo
0.015
name: 'SuperPointNet_pretrained'
model:
    name: 'SuperPointNet_pretrained' # 'SuperPointNet_gauss2'
    # name: 'SuperPointNet_gauss2' # 'SuperPointNet_gauss2'
    params: {
    }    
    batch_size: 1
    eval_batch_size: 1
    detection_threshold: 0.015 # 0.015
    nms: 4
    top_k: 600
    subpixel:
        enable: true
pretrained: 'pretrained/superpoint_v1.pth'
```


#### Train yaml

    

# 17-09-25
[[slides_for_3W_meeting]]

training:
```yaml
    workers_train: 1 # 16
    workers_val: 1 # 2

model:
    # name: 'magic_point'
    # name: 'SuperPointNet_heatmap'
    name: 'SuperPointNet'



    batch_size: 2 # 32
    eval_batch_size: 2 # 32
    learning_rate: 0.00001 # 0.0001
    detection_threshold: 0.015 # 0.015
    lambda_loss: 1 # 1
    nms: 4
    dense_loss:
        enable: false
        params:
            descriptor_dist: 4 # 4, 7.5
            lambda_d: 800 # 800
    sparse_loss:
        enable: true
        params:
            num_matching_attempts: 1000
            num_masked_non_matches_per_match: 100
            lamda_d: 1
            dist: 'cos'
            method: '2d'
    other_settings: 'train 2d, gauss 0.2'


retrain: True # set true for new model ??????
reset_iter: True # set true to set the iteration number to 0
train_iter: 200000 # 170000
validation_interval: 10000 # 2000 # 2000
tensorboard_interval: 10000 # 2000 # 200
save_interval: 10000 # 2000
validation_size: 6
```


pretrained: 'pretrained/superpoint_v1.pth'ng]]

[[3W_weekly_meeting]]

# 18-09-25
Ayer me la pasé estudiando MCP

# 19-09-25

Creo que el orden de prioridades sería:

1. Crear set C: nh0m100; th000075
2. Largar algun run
3. Preparar repos para trabajar en local Macos con los repos de las demos de superpoint y superglue
4. Preparar repo con AGENTS.md y plan de features

Estoy armando los tests para ver el thresh y el nhom para exportar

3 y 4 estan casi empezados. La idea sería trabajarlo en macos y correr los training en ubuntu. 
NO largué run (2) porque me quedé en 1 investigandolo a fondo

1 A lfinal hice un .md con las decisiones para tomar en el export y después me di cuenta que estaba aplicando la máscara. Tengo que hacer export con y sin máscara y cob y sin máscara specular (sin máscara no tendría sentido). La pregunta sería porque disparaba fuera de la máscara si ya estaba aplicada. Esto hay que revisarlo bien.

Como el downsize es 4 y la máscara estaba aplicada para un tamaño original, ahora los parametros quedaron grandes. Antes quizás aplicaba la máscar original y luego hacía downsize. Podría revisar eso también.

Ya hay una función para guardar la máscara que parecería estar bien. Endefinitiva debería entender que hace combine_heatmap

Conclusión para empezar el el lunes:

1. Revisar export.py: hay unos plots intermedios que todavía no enetendí. 
2. Hacer más chica la máscara o aplicarla antes de hacer el downsize.
3. Hacer exports con y sin máscara para ver las diferencias
4. Averiguar por qué dispararía fuera de la máscara si ya estaba aplicada.
5. Hacer el export con los parametros ya decididos
6. A partir de ahí ya podría ponerme con la demo y los runs para comparar el joint training.

Nota: acordarse que para cada export tengo que borrar la carpeta porque sino ve que ya tiene una label y despues lo saltea 

