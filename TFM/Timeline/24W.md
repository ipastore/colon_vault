# 9-02-26

Al correr from scratch, las métricas son parecidas. Esto puede significar varias cosas. 

1- Estoy matcheando mal en la demo. 
	a- Error en el código de la colon demo
	b- Error en el export de cudasift

2- Simplemente el descriptor de CUDASIFT es peor para matching.???
https://github.com/Celebrandil/CudaSift/issues/34
https://github.com/Celebrandil/CudaSift/issues/61

3- Revisar grayscale: lightglue está en grayscale, SLAM en green. Correr SLAM en grayscale o lightglue en green.

4- Todos los otros matches son peores o no pasarían la revisión geométrica.

5- Cambiar a popsift


Para esto puedo aplicar

1- Wrapper de CUDASIFT en python para entrenar con el entrenamiento de megadepth

2- Convertir CUDASIFT descriptor a OPENCV. 
https://github.com/Celebrandil/CudaSift/issues/42

See [[SIFT_vs_CUDASIFT]]

3- Mejorar la evaluación basada en un GT que viene del SLAM.



## Other stuff

Colmap --> CUDA_enabled --> SIFTGPU --> Lowe´s implementation
Colmap --> not CUDA_enabled --> VLFeat
Opencv?
CUDASIFT?

### Convert lg results to COLMAP db
https://github.com/cvg/LightGlue/issues/140

# Grayscale vs green channel
• CudaSIFT extraction happens in the Frame constructor, and it uses the green channel, not grayscale conversion.

  - In Frame::Frame, the input BGR image is resized into mImg, then extractChannel(mImg, mImg, channelGreen) is called before extraction. channelGreen is defined as 1.
    See src/cartography/Frame.cc:58-102 and include/cartography/Frame.h:115.
  - The extracted (single‑channel) mImg is passed directly into CudaSiftHandler::extractCudaSIFT2VectorKPs, which converts it to CV_32FC1 and runs ExtractSift.
    See src/cartography/Frame.cc:97-103 and src/CudaSiftHandler.cc:70-90.

# Scales and oris

  - CudaSIFT stores orientation in degrees. In the CUDA code it sets orientation = 360.0f*(...)/LEN and later converts to radians only for internal trig with theta = 2*pi/360*orientation.
    See Thirdparty/CudaSIFT/cudaSiftD.cu:208-211 and Thirdparty/CudaSIFT/cudaSiftD.cu:946-960.
  - The matcher also treats angles as degrees (wraps by 360.0f).
    See src/CudaSiftMatcher.cc:302-313.

  Scale vs sigma

  - The main extraction path used by the pipeline is Frame::Frame → CudaSiftHandler::extractCudaSIFT2VectorKPs.
    See src/cartography/Frame.cc:97-102.
  - In extractCudaSIFT2VectorKPs, the raw sift[i].scale is stored and used as the cv::KeyPoint::size, and sigma is computed from radius = scale * 0.5:
    sigma = k * (scale * 0.5) + alpha, then sigma2 = sigma * sigma.
    See src/CudaSiftHandler.cc:140-157.

  So for the extraction used in tracking, scale is treated like a diameter, and sigma is derived from scale/2 (effectively “2*sigma” if k=1 and alpha=0).

  One caveat: there is another helper (CudaSiftHandler::getVectorKeyPoints) that uses size = scale * 2 and sigma = k * scale + alpha, which treats scale as sigma. That is inconsistent with the extraction path above.
  See src/CudaSiftHandler.cc:717-733.

- Here’s exactly what gets fed into LightGlue for scales and oris, per the conversion code in src/DeepMatching.cc.

  Frame → LightGlue (convertFrameToTorch)

  - scales = F.mvfSiftScale[i]
  See src/DeepMatching.cc:258-274.

  - For real KF keypoints (with MapPoints):
      - scales = pKF->mvfSiftScale[i]
      - oris = pKF->mvKeys[i].angle * (pi/180) (radians)

  See src/DeepMatching.cc:312-348.

  - For virtual keypoints created from local map points:
      - orientation is hardcoded to 0.0 (placeholder)
      - scale is predicted from depth and set to pKF->mvScaleFactors[predictedLevel] (or 1.0 if not valid)

  See src/DeepMatching.cc:379-412.

  Where LightGlue is called

  - pLightGlue->forward(feats_*, feats_*) receives these tensors in DeepMatching::matching(...) and DeepMatching::matchLocalMap(...).
    See src/DeepMatching.cc:74, src/DeepMatching.cc:130, src/DeepMatching.cc:200.

  Yes, for the features coming from SLAM into LightGlue:

  - Orientations are converted to radians from OpenCV’s kp.angle (which is in degrees).
    See src/DeepMatching.cc:268-274 and src/DeepMatching.cc:345-348.
  - Scales are passed as stored:
      - For real Frame/KF keypoints: mvfSiftScale[i] is fed directly.
        See src/DeepMatching.cc:266-273 and src/DeepMatching.cc:342-348.
      - For virtual local‑map keypoints: scale comes from mvScaleFactors[predictedLevel] (or 1.0 if no valid level).
        See src/DeepMatching.cc:399-410.

  One nuance: virtual features use orientation = 0.0 (no actual orientation).
  See src/DeepMatching.cc:396-401.

  So the short answer is: oris are radians, scales are taken as-is from SLAM (except virtual features which use predicted scale and zero orientation).
## No keypoint offset

# 10-02-26

1- Estoy matcheando mal en la demo. 
	a- Error en el código de la colon demo: 
		Había un bug al cargar el .bin (en vez de 5 parametros leía 4 y luego se corría todo)
- 
	b- Error en el export de cudasift. NO

2- Simplemente el descriptor de CUDASIFT es peor para matching.???
https://github.com/Celebrandil/CudaSift/issues/34
https://github.com/Celebrandil/CudaSift/issues/61
PUEDE SER

3- Revisar grayscale: lightglue está en grayscale, SLAM en green. Correr SLAM en grayscale o lightglue en green. En slam creo que da ligeramente mejor con grayscale. En matching con green o grayscale es difícil porque no hay verificación geométrica

4- Todos los otros matchers son peores o no pasarían la revisión geométrica.? 

5- Cambiar a popsift.


TODO: 

1- Mejorar la evaluación basada en un GT que viene del SLAM.
2- Wrapper de CUDASIFT en python para entrenar con el entrenamiento de megadepth
3- Entrenar con más datos.


## Evaluación
Acá vendría bien también el wrapper, aunque también puedo cachear las features y listo. La Seq_050 es la que tengo que preparar nomás.

Evaluar en C3VD?

Steps: 
1. Make pairs.txt with 1500 pairs. Calibrate overlap con Seq_050. Puedo extraer 1500 pairs por bin y luego hacer uno que combine todos.
2. Extrac keyframes and depths de todas las imagenes y datasets anteriores.
3. No seq ue hacer con pairs_calibrated y views.txt

Preprocess la Seq_050 desde una nueva carpeta y checkear la calidad de archivos

### Setup for evaulation

LG
Setup: We use image pairs from the MegaDepth-1500 test set following the evaluation of LOFTR [68]. The test set contains 1500 image pairs from two popular phototourism destinations: St. Peters Square and Reichstag. The data was collected in a way that the difficulty is balanced based on visual overlap. We evaluate our method on the downstream task of relative pose estimation. We estimate an essential matrix both with vanilla RANSAC and LO-RANSAC [34], respectively, and decompose them into a rotation and a translation. The inlier threshold is tuned for each approach on the test data – we think that this makes the comparison more fair as we do not evaluate RANSAC itself. We compute the pose error as the maximum angular error in rotation and translation and we report its AUC at 5°, 10°, and 20°.

LOFTR
MegaDepth consists of 1M internet images of 196 differ-
ent outdoor scenes. The authors also provide sparse recon-
struction from COLMAP [40] and depth maps computed
from multi-view stereo. We follow DISK [47] to only use
the scenes of “Sacre Coeur” and “St. Peter’s Square” for
validation, from which we sample 1500 pairs for a fair com-
parison. Images are resized such that their longer dimen-
sions are equal to 840 for training and 1200 for validation.
The key challenge on MegaDepth is matching under ex-
treme viewpoint changes and repetitive patterns.


DISK
Hyperparameter selection. We rely on a validation set of two scenes: “Sacre Coeur” and “St.
Peter’s Square”. We resize the images to 1024 pixels on the longest edge, generate cycle-consistent
matches with the ratio test, with a threshold of 0.95. For stereo we use DEGENSAC [ 7], which
outperforms vanilla RANSAC [16], with an inlier threshold of 0.75 pixels

SUPERGLUE
Outdoor pose estimation – Section 5.3: For training on
Megadepth, the overlap score is the ratio of triangulated
keypoints that are visible in the two images, as in [19].
We sample pairs with an overlap score in [0.1, 0.7] at each
epoch. We evaluate on all 11 scenes of the PhotoTourism
dataset and reuse the overlap score based on bounding
boxes computed by Ono et al. [37], with a selection range
of [0.1, 0.4]. Images are resized so that their longest dimen-
sion is equal to 1600 pixels and rotated upright using their
EXIF data. We detect 2048 keypoints for both SIFT and Su-
perPoint (with an NMS radius of 3). The epipolar correct-
ness threshold is here 10−4. Other evaluation parameters
are identical to the ones used for the indoor evaluation.


## No estoy exportando las features virtuales
No tiene mucho sentido porque serían descriptores de una parte de la imagen que no existe. Para hacerlo se necesita de más pensamiento y diseño

## Armar docker con fork de CUDASIFT con python wrapper 


## Para la evaluación tengo que usar los mismos colmaps que las secuencias de SLAM

## TODO
- Investigar por qué se cargó mal el binario

- Agregar máscaras de especularidades on-the fly para cuando no se usa cache

- Pre-train con cudasift con revisitop
- Fine-tune con cudasift a Megadepth a partir de revisitop
- Fine-tune con cudasift cacheado a partir del cudasift finetuneado
- Armar evaluación en COLMAP muy parecido a Megadepth. Monti dice de agarrar las camaras pose y evaluar el error epipolar de los matches
- Pre-train con homografías en el colon + fine-tune con features cacheadas
- Preparar COLMAP dataset para evaluar y para train. Revisar como están guardadas las features y las depths.


- EVAL: Preparar estudio de las métricas para Monti
- EVAL: Integrar cudasift como extractor en gluefactory
-  TRAIN: Incorporar cudasift en el docker
- EVAL: Preparar el dataset de endomapper_COLMAP1500

# 12-02-26

[[metricas_eval_gluefactory]]

# 13-02-26

TODO: 

- Download datasets. DOING
- ADD py_CUDASIFT extractor in gluefactory
- EVAL: py_CUDASIFT in megadepth and other datasets.
- ADD py_CUDASIFT installation in docker
- ADD specular masks online to py_CUDASIFT
- TRAIN py_CUDASFIT with homographies

[[instalar_py_cudasift_docker]]
[[diferencias_sift_CUDASIFT]]

**Cosas para probar CUDASIFT vs SIFTGPU**
channel green vs grayscale
root_sift vs none: da un poco mejor root_sift salvo en rel_pose
Morlana parameters vs_ default
nuevos pesos
checkear pesos originales vs mi .pt
checkear offeset vs no offset: ya largué el primero sin offset. 


AHora largar un nuevo casi original pero con offset
despues los de morlana

Checking if the differences in the metrics are because the offset. Or maybe the octave=0 and octave-1?


## Making table for comparison

### Variables

checkpoint: outputs/training/long26_gt_pos_null_10_lg1e-03_lr5e-05_5bin/checkpoint_best.tar
chekpoint: null

extractor:
	name: extractors.sift
	name: extractors.superpoint
	name: gluefactory_nonfree.superpoint
	
	Note: Only for sift extractors
	backend: py_cudasift
	backend: py_colmap_cuda
	backend:open_cv
	
	
	detection_threshold: 0.4 
	detection_threshold: 0.0066667 
	
	rootsift: false
	rootsift: true
	
	
	num_octaves: 3 
	num_octaves: 4
	
	init_blur: 2.0
	init_blir: 1.0
	
	extractor_channel: green
	extractor_channel: grayscale
	  

matcher:
	name: matchers.lightglue

	features: sift
	features:supoeroint
	
	depth_confidence: -1
	
	width_confidence: -1
	
	filter_threshold: 0.1
	filter_threshold: 0.0001

# Conclusiones de comparacion de SIFT

[[comparaciones_sift_megadepth1500]]


# Homography pretrain for CUDASIFT
Training details: We extract 512/1024/1024 keypoints for SuperPoint/SIFT/DISK, and a batch size of 64. The initial learning rate is 0.0001, and we multiply the learning rate by 0.8 each epoch after 20 epochs. We stop the training after 40 epochs (6M image pairs), or 2 days with 2 Nvidia RTX 3090 (for SuperPoint). Our network achieves > 99% recall and > 90% precision on the validation and test set. We also observed that, for fine-tuning, one can stop the pre-training after just one day with only minor losses. We also experimented with sampling images from MegaDepth [38] for homography pre-training, and could not observe major differences. Strong photometric augmentations and perspective changes are crucial for training a robust model.

  python -m gluefactory.train <exp_name> --conf <config.yaml> --distributed

# FineTuning in Megadepth
Training details: Weights are initialized from the pretrained model on homographies, Training starts with a learn ing rate of 1e-5 and we exponentially decay it by 0.95 in each epoch after 10 epochs, and stop training after 50 epochs (2 days on 2 RTX 3090). The top 2048 keypoints are extracted per image, and we use a batch size of 32. To speed-up training, we cache detections and descriptors per image, requiring around 200 GB of disk space.

# 15-02-26

A tener en cuenta: 
Como se evaluaría con nms=3? Quizás mejora py_cudasift
Porque en realidad viene más del extractor que del matcher ahora

Averiguar bien si queremos un matcher más preciso o más recall. Debería estudiar bien si entrenando con un th más bajo, afecta a la predicción luego. Es decir, si entreno con lg = 0.0001, luego me dará siempre más matches pero con menor probabilidad?

Estoy corriendo homography con py_cudasift y pycolmap.
Debería hacer lo mismo con megadepth from scratch. 
Luego hacer lo mismo con megadepth from homography.

Pensé también enxportar las features de otra manera. Quizás con un th más alto para no sacar tantos keypoints y después tener que filtrar con scores. 

Investigar si en gluefactory puedo cargar mapas de colmap fácilmenet con el data loader




```bash
ps -u $USER -o pid,cmd | grep 'py_cudasift_scratch+lightglue_megadepth'
```

  
```bash
nvidia-smi --query-compute-apps=gpu_uuid,pid,process_name,used_memory --format=csv,noheader | grep -E '145503|145504|145505|...'
```

Py_cudasfit_homography
| N/A   46C    P0    69W / 300W |  27452MiB / 32510MiB |      0%      Default |

Megadepth 
| N/A   59C    P0   291W / 300W |  31658MiB / 32510MiB |    100%      Default |

py_colmap_cuda_homography
| N/A   50C    P0    74W / 300W |  27694MiB / 32510MiB |     12%      Default |


Py_cudasfit termina disparando en el cielo, mucho y nos e por queé