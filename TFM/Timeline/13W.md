# 24-11-25
EStoy corriendo un entrenamiento de homografías

Cosas a hacer: 
- Ver tensorboard: OK
- Cargar pesos en demo: OK
- Modificar demo para dejra una linea con el lightglue reentrenado. Para la demo pouedo parametrizar los modelosque quiero que aparezcan en cada columna. OK 
- Ruben me dijo que tengo que empezar con el 3D. Para eso primero necesito poder exportar bien las feats de CUDASIFT desde el SLAM. DOING
- Una vez que tengo las features, tengo que armar una lógica para cargar los matches positivos y los negativos serían contra los otros. DOING
- Algo que podría hacer es comparar como funciona lightglue con todos los extractores pero principalment pycolmap SIFT GPU y CUDASIFT (pero eso me va a llevar más tiempo ahora). TODO
# 25-11-25

Meeting con Ruben y Morlana:

- Exportar las features desde el SLAM, además puedo exportar la depth, donde para cada punto tengo su depth. Luego podría hacer algo muy parecido a lod e megadepth. Sin embargo puedo empezar solamente con lo de los positives. 
- Los hard negatives son punteos INMATCHEABLES (por eso se tiene cuidado al marcarlos)
- Saco los positives directamente del matching. 
- Sacar las features solo de los KF: mvx tengo las coordenadas; mvmapoints (punteros con los matches)
- Ruben dice que debería andar bien con el pinhole, sin kannala brandt. Esto es otro todo para el futuro. 
- Luego otro TODO para el futuro que se debería bajar el lr de 1e-04 a 1e-05 para empezar.

# 26-11-25
Ayer vi como correr el Dockerfile pero interactivamente. La solución es devcontainer donde corres la imagen como en modo interactivo pero con la funcionalidad de Vscode, de esta manera puedo debuggear con el debugger de VSCODE

ay instalé in devcontainer para release mode y para debug mode

# 27-11-25

Empiezo a debugear para exportar las features de CUDASIFT

pensé que en images no había matches 2D-3D pero al final si, checkeado:

Además hay que sumar la función a Keyframe.h, agregar fstream con un include y limpiar el stream antes de entrar a la funcion
```cpp
// Clear the debug file so we start fresh
std::ofstream fDebug("2D-3D_matches.txt", std::ios::trunc);
fDebug.close();
```


```cpp
std::string KeyFrame::DEBUG_getPointsColmapFormat()
{
// Open debug file in append mode
std::ofstream fDebug;
fDebug.open("2D-3D_matches.txt", std::ios::app); 

if (fDebug.is_open())
{
fDebug << "KeyFrame ID: " << mnId << " (Frame ID: " << mnFrameId << ")" << std::endl;
fDebug << "Index | X | Y | MapPointID" << std::endl;

}

// FORMAT: POINTS2D[] as (X, Y, POINT3D_ID)
stringstream ss;
ss << fixed << setprecision(15);
for (size_t i = 0; i < mvKeys.size(); ++i)
{
float x = mvKeys[i].pt.x;
float y = mvKeys[i].pt.y;
int mappointID = -1;
MapPoint* pMPi = mvpMapPoints[i];
if(pMPi && !pMPi->isBad())
{
mappointID = pMPi->mnId;
}

if (fDebug.is_open())
{
fDebug << i << " " << x << " " << y << " " << mappointID << std::endl;
}
 
ss << x << " " << y << " " << to_string(mappointID) << " ";
}

if (fDebug.is_open())
{
fDebug << "--------------------------------------------------" << std::endl;
fDebug.close();
} 
string text = ss.str();
if(!text.empty())
text.pop_back(); // Erase last space
return text;
}
```

# 28-11-25
Ayer exporté las features de Cudasift. Las podría debugear o hacer un test en python a ver si 

## Plan: Verify 2D-3D COLMAP Correspondences Test

Create a unit test function that validates bidirectional consistency between 2D keypoint observations in `images.txt` and 3D point tracks in `points3D.txt`. The test will parse both files into dictionaries, then verify that every 2D→3D reference has a matching 3D→2D back-reference.

### Steps

1. **Create parser functions** in  new `tools/test_colmap_correspondences.py` following existing patterns from [base_dataset.py](vscode-file://vscode-app/Applications/Visual%20Studio%20Code.app/Contents/Resources/app/out/vs/code/electron-browser/workbench/workbench.html) (skip headers, parse line-by-line, handle triplets for points2D and pairs for tracks)
    
2. **Build data structures** with two dictionaries: `images_dict[IMAGE_ID] = {'points2D': [(x, y, point3D_id), ...]}` and `points3D_dict[POINT3D_ID] = {'track': [(image_id, point2D_idx), ...]}`
    
3. **Implement forward verification** loop through all images, enumerate points2D to get `point2D_idx`, skip if `point3D_id == -1`, assert `(image_id, point2D_idx)` exists in `points3D_dict[point3D_id]['track']`
    
4. **Implement backward verification** loop through all points3D tracks, for each `(image_id, point2D_idx)` pair, assert `images_dict[image_id]['points2D'][point2D_idx][2] == point3D_id`
    
5. **Error reporting detail**  Collect all mismatches into a list before asserting Comprehensive 

# Diferencias para marcar con Megadepth vs Endomapper

[[things_to_adapt_from_megadepth]]