# 29-09-25
Preguntas teoricas que quiero resolver

**COmo se calcula la loss? porque con D da mas bajo? hibiera esperado mas alto**
**esta normalizado el heatmap?**
El heatmap está normalizado con una softmax. La loss se calcula por pixel porque está dividido por la cantidad de valid_pixels que te quedaron. La loss es la suma de la cross entropy de los 2 detectores más la loss de descriptor (con lambda), donde los puntos matcheados deberían tener buen score de correlacion

**Como funciona el entreanmiento? como es el paso a paso? para que siven las labels y las warped labels?. Explicar bien ese mecanismo**
Primero se exportan las labels, luego se aplica la loss segun el batch size. La loss es calculada como antes. Entonces a lo que apuntaba esta pregunta es:
Si o si debería aplicar la máscara para exportar las labels. 
En teoría lo mejor para el train_joint sería no aplicar máscara en ninguna imagen. 

Como la loss está bastante bumpy, debería hacer el tema de las mascaras con el C

aprender a usar la dgx

# 30-09-25

Estoy corriendo el 3er train_joint con D para explicar el funcionamiento de las máscaras
Para esto debo ver bien las visualizaciones

Analizar si tengo que hacer otro train más corto con C para demostrar el comportamiento de las mascaras. 

- Usar dgx: 
		- Tengo el container de docker armado?
		- Debería preparar los exports en local y luego hacer el train_joint en DGX

Me queda hacer el ablation study the bajar mucho el th. Lo puedo hacer
Y me queda afuera el weight decay creo, pero lo podría probar con C

Hacer presentacion

Aprendí que si corro mas de una vez el test: con leon model dio exactamente igual

squares in heatmap: analyze ofverfitting with going through all the checkpoints

# 01-10-25
[[5W_weekly_meeting]]

# 02-10-25
Me pongo con el TODO y lo de la weekly meeting: empezar con el entrenamiento con más datos. Tener cuidado con la secuencia 24

Al final tenía que hacerlo con las secuencias de COLMAP, no con el "all_sequences". Son todas imagenes 1350, 1012, con la mascara anterior. Ahora estoy haciendo el export y ya largo el training. 

En macos py38-sp es para la demo
py38-colonsp es para el ColonSuperpoinTorch (revisado por fecha y por cantidad de dependencias)
ya puedo debuggear con py38

Fijarse el tema del tamaño del container por la gpu.
Lo que pude hacer es configurar el container en macos y pude correr un export con pocas homogragfías: por eso el tamaño del container. El tema es que está corriendo engpu, habría que ver como corre en ubuntu. 

revisar la flag --shm-size=1g

```bash
docker run --rm --shm-size=1g \
  -v $(pwd)/datasets:/workspace/ColonSuperpoinTorch/datasets \
  -v $(pwd)/runs:/workspace/ColonSuperpoinTorch/runs \
  -v $(pwd)/logs:/workspace/ColonSuperpoinTorch/logs \
  -v $(pwd)/configs:/workspace/ColonSuperpoinTorch/configs \
  -v $(pwd)/models:/workspace/ColonSuperpoinTorch/models \
  colonsp:dev \
  python export.py export_detector_homoAdapt configs/superpoint_colon_export_test.yaml test --outputImg > export_log.out 2>&1
```

  
Tambien pude correr el debug co VsCode con py38sp

Tener en cuenta que en DGX puedo correr con varias GPUs, debería ver eso. En vez de correr varios en paralelo, lanzar uno con todas las GPUs.

PLAN para la proxima semana: 
revisar el tema del tamaño del container, llevarlo a ubuntu, correrlo en local y luego ver como lo puedo montar en la DGX

Estudiar superpoint.

