# 26-12-26

Pasando secuencias, después preprocesar y largar algo en la dgx

Avisar a Javi para que largue las otras

- Preprocesar
- rsync a dgx
- Runes: hacerlo sin splits de overfit, solo 8 pairs
	- lr1e-06_5bin
	- lr1e-06_nobin
	- lr1e-05_5bin


## move the Seq_* dir upwards
```bash
for d in Seq_*; do [ -d "$d/$d" ] && mv "$d/$d"/* "$d/" && rmdir "$d/$d"; done
```

### Check how many wc
```bash
for d in Seq_*; do echo -n "$d: "; ls -1 "$d" | wc -l; done
```

#  30-12-26
Tuve un error al hacer rsync y cerrar la mac. No sirve nohup ni bg. Habría que usar tmux
Por alguna razón no logea los training de la dgx: habría que bajar el num_per_scene? porque son más escenas? Además bajar log y eval para ver que realmente funcionen en la dgx. En student creo que ya está funcionando

corre sin los badfiles

revisar en la dgx cuando vuelva. Podría o pushear los cambios con los try y exception o salmente seguir adelante


Seq_010_map52.npz	Error accessing key 'descriptors_per_image': Bad CRC-32 for file 'descriptors_per_image.npy' | zipfile.testzip bad member: descriptors_per_image.npy


ESTOS DOS MAPAS NO ESTAN EL A DGX PORQUE LOS BORRE
Seq_007_map128.npz Error accessing key 'descriptors_per_image': Bad CRC-32 for file 'descriptors_per_image.npy'

Seq_007_map148.npz Error accessing key 'descriptors_per_image': Bad CRC-32 for file 'descriptors_per_image.npy'
ESTOS DOS MAPAS NO ESTAN EL A DGX PORQUE LOS BORRE

Seq_018_map112.npz	Error accessing key 'descriptors_per_image': Bad CRC-32 for file 'descriptors_per_image.npy' | zipfile.testzip bad member: descriptors_per_image.npy

[01/30/2026 23:00:29 gluefactory.datasets.endomapper ERROR] Failed to read NPZ content: path=/workspace/data/slam-results-nacho/processed_npz/Seq_009_map80.npz seq_map=Seq_009_map80 image_name=15750 idx=11



Ahora modifiqué el check con test zip file. Esto debería andar bien. Estoy corriendo en dgx con workers 8 y en local con workers 0. Lo voy a dejar un rato para quedarme tranquilo y después debería ver si pusheo o directamente largo los entrenamientos. 

Luego tengo que ver por qué el error de escritura CRC, debe ser algo del tamaño del npy, ver algo del formato estilo zip64.
Este es probablemente el error: sumar workers con np.savez
https://github.com/numpy/numpy/issues/18124

Para agarrar el problema debería pushear y volver a buildear la imagen 

Pusheado, me dio un error de otro mapa. 

- Correr mapas que tuve que borrar con un preprocess por npz
- Rsync
- Correr con los mapas que borré

# 31-12-26

Creo que anda bien debería largar lo de los bines perooo... me da un poco de paja? NO

Listo,  corrido con las val_pairs pero sin los mapas nuevos. Lo que queda ahora es sumar las otras seqs y seguir adelante. Seqs para correr: 

- Seq_005
- Seq_008
- Seq_013
- Seq_015
- Seq_025
- Seq_014
- Seq_026

Proceso.
-1 Exportar mapas, mover con el script y revisar
-2 Preprocesar 
-3 Check
-4 rsync
-5 Check, este no lo estoy haciendo porque no quería correr un .sh solo pero ya fue. 


OTRA COSA: wrapper de CUDASFIT. TEngo que ver como hago compatibles las imagenes de docker para poder utilizar gluefactory y cudasift al mismo tiempo con CUDA support. 

OTRA COSA: hacer una comparaión con cudasift con los pesos oficiale y los mios fine-tuneados