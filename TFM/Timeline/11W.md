# 10-10-25
Me bajé el dataset de Megadepth, ver si entra en memoria, empezzar a ver com oadaptar para el endomapper dataset.
Ya corre bien con Megadepth el fine-tuning. 
Próximos pasos: adaptar dataset + imagen de docker + AGENTS.md

# 11-10-25
Estoy tuneando la imagen de docker, todavía en student para ver si l puedo hcaer correr en la dgx

Comandos que voy usando: 

**Para evaluar hpatches**
```bash
docker run --user $(id -u):$(id -g) --gpus all -it --shm-size=8g   -v /home/student/glue-factory-colon:/workspace   -v /media/student/HDD/nacho/glue-factory/data:/me
dia/student/HDD/nacho/glue-factory/data   gluefactory:cuda11.4   bash -c "cd /workspace && conda run -n gluefactory python -m gluefactory.eval.hpatches --conf superpoint+lightglue-official --overwrite"
```


**Para evaluar hpatches pero manteniendo los paths**
```bash
docker run --user $(id -u):$(id -g) --gpus all -it --shm-size=8g   -v /home/student/glue-factory-colon:/workspace   -v /media/student/HDD/nacho/glue-factory/data:/workspace/data   gluefactory:cuda11.4   bash -c "cd /workspace && conda run -n gluefactory python -m gluefactory.eval.hpatches --conf superpoint+lightglue-official --overwrite"
```

**Para entrenar en megadepth**

```bash
docker run --user $(id -u):$(id -g) --gpus all -it --shm-size=8g   -v /home/student/glue-factory-colon:/workspace   -v /media/student/HDD/nacho/glue-factory/data:/media/student/HDD/nacho/glue-factory/data   gluefactory:cuda11.4   bash -c "cd /workspace && conda run -n gluefactory python -m gluefactory.train sp+lg_megadepth --conf gluefactory/configs/superpoint+lightglue_megadepth.yaml"
```


Ahora estoy rebuildeando l aimagen que andaba bien para dejar el path intacto y poder montar el /media/ con Docker. Luego tengo que seguir con el push de los cambios para poder pullear desde la DGX y luego hacer un rsync con los datasets

Listo la imagen de Docker, estoy pasando Megadepth que tarda un montonazo

# 12-11-25
DEbugging train sift+lg in megadepth. 

Donde puedo empezar a adaptar: [[CUDASIFT+lg_endomapper_maps]]

No tengo lugar en la DGX para cargar Megadepth, no importa porque no lo debería usar. 

tengo un quilombo barbaro para correr con sift
Ya instalé pycolmap con cuda, pensando que me iba a sacar de encima todos los problemas pero no
Tambien puedo probar con kornia_extractor de sift o también opencv
Al instalar un nuevo pucaolmap con CUDA, cambié las options por otra manera de llamarlos, además 
como scores es None, no tengo como fijar el max_num_keypoints. Y si no hago eso (en el config) me tira esto: RuntimeError: stack expects each tensor to be equal size, but got [1642, 2] at entry 0 and [1858, 2] at entry 1

Entonces tengo que dejar la flag activada pero todavía tengo que pasar o max_num_keypoints o tratar de agarrar los max. 
  File "/home/student/glue-factory-colon/gluefactory/models/extractors/sift.py", line 230, in _forward
    p = self.extract_single_image(img)
  File "/home/student/glue-factory-colon/gluefactory/models/extractors/sift.py", line 211, in extract_single_image
    if pred["keypoint_scores"] is not None:
KeyError: 'keypoint_scores'

# 13-11-25
REunion de SLAM
traté de arreglar el sift extractor pero no pude

# 14-11-25
Arreglué SIFT extractor: había algunos bugs con forece_num_keypoints, etc
Ahora estoy bajando dataset de homograhpy
Preguntar a morlana como puedo exportar las features
Preguntar a info tech como puedo hacer para guardar mas espacio en la DGX

Esto es lo que me contestó Morlana para exportar las features: 

```bash
Buenas nacho, sobre la primera pregunta, yo me cree un ejemplo sencillo para sacar las features de un conjunto de imágenes. Te lo puedo pasar, luego lo busco

Y de la segunda parte, le echo un vistazo tb. Puedes ir mirando que está haciendo, es la función Save3DColmaoFormat (algo así). Parece que los tracks no los está exportando (editado) 

Tendrás que meter lo de las features en esa función para tener los descriptores, que es lo único que no se está exportando ahora mismo

```

Beaware of the OPENCV FISHEYE model

AL final se me desconectó el cable de internet